/* ============================================================
   Template do config.js — copie este arquivo para `js/config.js`
   e preencha os valores localmente (NUNCA commitar js/config.js).

   Em produção, o GitHub Actions injeta esses valores a partir
   dos secrets do repositório no momento do deploy.
   ============================================================ */

const SHEETS_WEB_APP_URL = "";  // URL /exec do Apps Script
const SHEETS_TOKEN       = "";  // token compartilhado com o Apps Script
const PIX_KEY            = "";  // telefone do PIX, só dígitos. Ex: "31999999999"
const PIX_NAME           = "";  // nome do titular da conta PIX
